services:
  matcher:
    build:
      context: ..
      dockerfile: ops/Dockerfile
    entrypoint:
      - python
      - -m
      - harness.cli
      - --input-dir=/app/src/papers
      - --soon-days=31
    volumes:
      - ../src:/app/src
      - ./.env:/app/ops/.env:ro
      - ./.paperflow.local.toml:/app/ops/.paperflow.local.toml:ro
    env_file:
      - .env
    environment:
      MAX_PARALLEL: ${MAX_PARALLEL:-1}
      MAX_RALPH: ${MAX_RALPH:-8}
