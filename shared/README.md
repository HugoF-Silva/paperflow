# shared

Code and utilities used by both `journal-matcher` and `paper-formatter`.

## Planned modules

- **`parsers/`** — `.docx` ingestion: extract body text, metadata (title, authors, abstract, keywords), figures, tables, and references
- **`models/`** — shared data schemas: `Paper`, `Journal`, `SubmissionWindow`, `TemplateSpec`
