[project]
name = "academia-perks-harness"
version = "0.1.0"
description = "Dev harness + bundled program for the academia-perks venue-matcher plugin."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = [
    "claude-agent-sdk>=0.2.105",
    "python-docx>=1.2.0",
    "psutil>=7.2.2",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["harness*"]
namespaces = true

[tool.pytest.ini_options]
pythonpath = [".", "skills/venue-matcher/scripts/venue_matcher"]
testpaths = ["tests"]
